@model IEnumerable<GymManager.Models.Plan>

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0 text-gray-800"><i class="bi bi-tags-fill text-primary"></i> Planes de Membresía</h1>
    <a asp-action="Create" class="btn btn-primary rounded-pill px-4 shadow-sm">
        <i class="bi bi-plus-lg"></i> Nuevo Plan
    </a>
</div>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-4 mb-4 animate__animated animate__fadeInUp">
            <div class="card shadow border-0 h-100">
                <div class="card-body text-center p-4">
                    <div class="display-6 fw-bold text-primary mb-2">S/ @item.precio</div>
                    <h5 class="card-title fw-bold text-uppercase">@item.nombre_plan</h5>
                    <p class="text-muted">Duración: <span class="fw-bold">@item.duracion_dias días</span></p>
                    <hr />
                    <div class="d-grid gap-2">
                        <a asp-action="Edit" asp-route-id="@item.plan_id" class="btn btn-outline-secondary btn-sm">
                            <i class="bi bi-pencil-square"></i> Editar Plan
                        </a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>